{
  "name": "@xtb-xcontrol/server",
  "version": "0.1.0",
  "description": "Language Server for xTB xcontrol files",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "echo \"No tests yet for server package\""
  },
  "dependencies": {
    "@xtb-xcontrol/shared": "*",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.2"
  }
}
